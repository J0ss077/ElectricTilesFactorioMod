local fragmnt_001 = require("scripts.fragments.fragment-001")

return function(surface_name, old_tiles, tile_prototype)
    --
    fragmnt_001(surface_name, old_tiles, tile_prototype)
    --
end
